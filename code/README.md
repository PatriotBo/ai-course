# Code Workspace

这里保存课程配套代码。

建议目录：

```text
code/
├── llm-api-service/
├── rag-knowledge-assistant/
├── tool-using-agent/
├── langgraph-workflow/
├── mcp-server/
└── final-project/
```

原则：

1. 每个子项目必须有 README；
2. 必须有 `.env.example`；
3. 必须能本地运行；
4. 必须记录运行命令和预期输出；
5. 生产相关代码需要包含日志、超时、重试和错误处理。
